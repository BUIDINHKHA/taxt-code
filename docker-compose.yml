version: "3.8"

services:
  smtp-statement:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    env_file:
      - api/.env.docker
    volumes:
      - ./api:/app
    restart: unless-stopped
    networks:
      - backend
networks:
  backend:
    name: backend
